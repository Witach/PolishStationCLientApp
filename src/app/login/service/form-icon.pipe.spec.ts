import { ValidFormIconPipe } from './form-icon.pipe';

describe('FormIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidFormIconPipe();
    expect(pipe).toBeTruthy();
  });
});
